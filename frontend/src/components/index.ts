/**
 * Shared components — placeholder.
 *
 * Design system components will be added per phase:
 *   Button, Input, Card  — Phase 3
 *   Map                  — Phase 5
 *   Leaderboard          — Phase 6
 */

export {};
