/**
 * PostgreSQL connection stub.
 * Full wiring (Pool creation, health check, migration runner) will be added in Phase 2.
 *
 * The `pg` package is installed and ready to use — import Pool from 'pg' when needed.
 */

// Phase 2 TODO: Initialize pg.Pool using env.DATABASE_URL or individual env vars.
// Example:
//
// import { Pool } from 'pg';
// import { env } from './env';
//
// export const pool = new Pool({
//   connectionString: env.DATABASE_URL,
// });
//
// export async function connectDb(): Promise<void> {
//   await pool.query('SELECT 1'); // verify connection
//   console.log('PostgreSQL connected');
// }

export {};
