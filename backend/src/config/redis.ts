/**
 * Redis connection stub.
 * Full wiring (IORedis client, pub/sub setup) will be added in Phase 2.
 *
 * The `ioredis` package is installed and ready to use — import Redis from 'ioredis' when needed.
 */

// Phase 2 TODO: Initialize ioredis client using env.REDIS_URL.
// Example:
//
// import Redis from 'ioredis';
// import { env } from './env';
//
// export const redis = new Redis(env.REDIS_URL);
//
// redis.on('connect', () => console.log('Redis connected'));
// redis.on('error', (err) => console.error('Redis error:', err));

export {};
