import { Router } from 'express';

/**
 * Territories routes placeholder.
 * Implemented in Phase 5 (territory capture algorithm).
 *
 * Planned endpoints:
 *   GET /api/territories          (map viewport query)
 *   GET /api/territories/:tileId  (single tile detail)
 *   GET /api/territories/user/:id (tiles owned by user)
 */
export const territoriesRouter = Router();
