import { Router } from 'express';

/**
 * Users routes placeholder.
 * Implemented in Phase 3 (user profiles).
 *
 * Planned endpoints:
 *   GET    /api/users/:id
 *   PATCH  /api/users/:id
 *   DELETE /api/users/:id
 */
export const usersRouter = Router();
