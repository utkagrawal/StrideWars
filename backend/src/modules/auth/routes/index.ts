import { Router } from 'express';

/**
 * Auth routes placeholder.
 * Implemented in Phase 3 (JWT authentication).
 *
 * Planned endpoints:
 *   POST /api/auth/register
 *   POST /api/auth/login
 *   POST /api/auth/refresh
 *   POST /api/auth/logout
 */
export const authRouter = Router();
