import { Router } from 'express';

/**
 * Runs routes placeholder.
 * Implemented in Phase 4 (run recording).
 *
 * Planned endpoints:
 *   POST   /api/runs
 *   GET    /api/runs/:id
 *   GET    /api/runs (list for current user)
 *   DELETE /api/runs/:id
 */
export const runsRouter = Router();
